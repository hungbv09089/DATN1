<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="20dp"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".ui.activity.VerifiedActivity">
    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="124dp"
        android:layout_marginBottom="30dp"
        app:srcCompat="@drawable/logo_splash" />
    <TextView
        android:text="XÁC NHẬN EMAIL"
        android:gravity="center"
        android:textSize="25dp"
        android:fontFamily="@font/roboto_bold"
        android:textColor="#1c8eff"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <TextView
        android:gravity="center"
        android:text="Link xác nhận đã được gửi tới Email của bạn!"
        android:layout_marginBottom="20dp"
        android:textColor="#1c8eff"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <TextView
        android:gravity="center"
        android:text="Vui lòng xác nhận đăng nhập Email \n và xác nhận để hoàn thành đăng ký!"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <Button
        android:onClick="onClickSubmit"
        android:id="@+id/btnLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:background="@drawable/bg_btn_login"
        android:fontFamily="@font/roboto_bold"
        android:text="Xác nhận"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="9pt" />
</LinearLayout>